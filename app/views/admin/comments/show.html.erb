<h1>コメント詳細</h1>
<div class="text-right mb-3">
  <%= link_to '削除', admin_comment_path(@comment), method: :delete, "data-confirm" => "本当に削除しますか？", class: "btn btn-dark" %>
  <%= link_to '戻る', admin_comments_path, class: "btn btn-secondary" %>
</div>
<div class="row">
  <div class="col-lg-6">
    <p class="py-3 pl-1" style="background-color: rgba(165, 143, 134, 0.2);">
      <strong>コメント詳細</strong>
    </p>
    <p>
      コメント者：
      <%= @comment.user.name %>
    </p>
    <p class="wrap-text">
      ◆ コメント内容 ◆<br>
      <%= simple_format @comment.body %>
    </p>
    <p>
      コメント日：
      <%= @comment.created_at.strftime('%Y/%m/%d') %>
    </p>
    <p>
      同投稿に対するコメント数：
      <%= @post.comments.count %> 件
    </p>
  </div>
  <div class="col-lg-6">
    <p class="py-3 pl-1" style="background-color: rgba(165, 143, 134, 0.2);">
      <strong>コメントした投稿</strong>
    </p>
    <p>
      タイトル：
      <%= link_to @post.title, admin_post_path(@post) %>
    </p>
    <p>
      投稿者：
      <%= link_to @post.user.name, admin_user_path(@post.user) %>
    </p>
    <p class="wrap-text">
      ◆ 投稿内容 ◆<br>
      <%= simple_format @post.body %>
    </p>
    <p>
      投稿日：
      <%= @post.created_at.strftime('%Y/%m/%d') %>
    </p>
    <p>
      投稿更新日：
      <%= @post.updated_at.strftime('%Y/%m/%d') %>
    </p>
  </div>
</div>
